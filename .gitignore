# secrets
.env

# python / venv
.venv/
__pycache__/
*.pyc

# rust / native extension build artifacts (Cargo.lock IS tracked for reproducible builds)
target/
*.so
*.rs.bk

# caches
.lake_cache/
.coinapi_cache/

# raw data archives (parquet / csv.gz) — keep out of git
*.csv.gz
*.parquet
data/raw/
coinapi_rate_limits/

# parity / recon reports (generated; can be large) — keep out of git
data/reports/
data/processed/

# generated planning artifacts (quality-map batch plans etc.) — regenerate, don't commit
data/tmp/
